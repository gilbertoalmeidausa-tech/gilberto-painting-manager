[build]
buildCommand = "pnpm --filter @painting/shared build && pnpm --filter @painting/client build"

[deploy]
startCommand = "npx serve -s client/dist -l $PORT"
healthcheckPath = "/"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
